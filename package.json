{
  "name": "hackman",
  "version": "1.0.0",
  "description": "A user-friendly website that allows users to answer a questionnaire, detect symptoms, predict the likely disease, and recommend as well as book a doctor’s appointment.",
  "main": "index.js",
  "scripts": {
    "start": "python -m venv venv && source venv/bin/activate && pip install -r requirements.txt && python views/app.py && npx nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.21.0",
    "express-session": "^1.18.0",
    "mongoose": "^8.6.2"
  }
}
